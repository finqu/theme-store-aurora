{% comment %}
	Renders a category list item

	Accepts:
	- category: {Object} category Liquid object (required)
	- currentCategory: {Object} category Liquid object (required)

	Usage:
	{% render 'sidebar-menu-item', category: category, currentCategory: category %}
{% endcomment %}

{%- if category.is_visible and category.categories.size > 0 -%}
	{%- assign hasSubCategories = true -%}
{%- endif -%}

{%- if hasSubCategories and category.id == currentCategory.id or category.child_current -%}

	<li class="sidebar-menu-item{% if category.id == currentCategory.id %} active-item{% endif %}{% if category.id == currentCategory.id or category.child_current %} has-active-item{% endif %}">

		{%- if category.id == currentCategory.id -%}

			<span class="sidebar-menu-item-text">
				{{- category.title -}}
			</span>

		{%- else -%}

			<a href="{{ category | link }}" class="sidebar-menu-item-link">

				<span class="sidebar-menu-item-text">
					{{- category.title -}}
				</span>

			</a>

		{%- endif -%}

		{%- if category.categories.size > 0 -%}

	        <ul class="sidebar-menu-subcategory-container">

	            {%- for item in category.categories -%}

	                {%- render 'sidebar-menu-item', category: item, currentCategory: currentCategory -%}

	            {%- endfor -%}

	        </ul>

	    {%- endif -%}

	</li>

{% elsif category.is_visible -%}

	<li class="sidebar-menu-item{% if category.id == currentCategory.id %} active-item{% endif %}">

		{%- if category.id == currentCategory.id -%}

			<span class="sidebar-menu-item-text">
				{{- category.title -}}
			</span>

		{%- else -%}

			<a href="{{ category | link }}" class="sidebar-menu-item-link">

				<span class="sidebar-menu-item-text">
					{{- category.title -}}
				</span>

			</a>

		{%- endif -%}

	</li>

{%- endif -%}